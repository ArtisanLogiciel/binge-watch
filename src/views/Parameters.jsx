import React from 'react';
import Layout from "../components/Layout";

export default function Parameters({props}) {
    
    return (
        <Layout >
            vous pourrez regler les parametre ici
        </Layout>
    )
}